# 第三方声明

## 厂商与本地资料（不入仓）

以下目录可能包含版权或未公开硬件资料，**本仓库不跟踪**（见 `.gitignore`）：

| 路径 | 说明 |
|------|------|
| `docs/路舵机控制器资料/` | 厂商软件工具、教程与示例工程等 |
| `docs/schematics/` | 板级原理图 |
| `docs/specs/` | 器件数据手册等 PDF |

请从厂商或项目方获取授权副本；再分发本仓库时勿将上述目录一并发布。

## Python 依赖

运行时依赖（PySide6、pyserial、hidapi）及开发依赖（pytest、PyInstaller）遵循各自上游许可证。安装时由包管理器解析；本仓库源码本身为 MIT。

## 协议对照

串口／HID 指令字符串表对照 Zide 等厂商上位机行为实现，用于互操作；**不包含**厂商闭源程序本体。
