# Task 8 Report
- 新增 `GroupsPage`，支持动作组新增、插入、删除、复制、粘贴和自动重编号。
- 单击动作组同步到 `ControlPage.apply_pose`，双击通过串口发送组合动作指令。
- 接入 `SequenceRunner` 在线、脱机、下载和取消，并接入开机动作设置/清除协议。
- `MainWindow` 现在持有共享 `Project`，控制页和动作组页始终引用同一实例。
- 新增打开、保存、另存为文件操作，使用 `load_ini`/`save_ini` 并显示当前文件名。
- 控制页姿态和时长会同步为新增/插入动作组的数据。
- 新增动作组与工程文件 UI 测试，所有新增控件均在测试结束时关闭并延迟删除。
- 验证：`python -m pytest -q` → `43 passed`，Windows 退出码 0。
- 实际 `config_bes.ini` 成功载入 20 组并生成 G0000–G0003 在线指令。
- 未连接硬件，未执行真实控制板下发验证；未实施 Task 9+。
